import React from 'react';

// This component is now a placeholder and does not render anything.
// We are using process.env.API_KEY directly to avoid the "Paid Key" project dialog.
const KeySetupModal: React.FC<any> = () => null;

export default KeySetupModal;